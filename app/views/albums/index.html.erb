<% @titre = "Music" %>
<div id="albumlists" class="span-10">
<%= render(:partial => "album_list", :locals => {:album_list => @albums, :boxtitle => "New Albums"}) if ! @albums.blank? %>
<%= render(:partial => "album_list", :locals => {:album_list => @unapproved_albums, :boxtitle => "Unapproved Albums", :page_param => :unapproved_page}) if ! @unapproved_albums.blank? %>
</div>

<div id="mainalbum" class="span-10 last">
<%# render(:partial => "album_details", :locals => {:boxtitle => "Newest Addition" || @boxtitle, :album => @album || @albums.first }) if @album || @albums.first %>
</div>
